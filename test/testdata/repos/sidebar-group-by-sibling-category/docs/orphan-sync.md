---
title: Orphan Sync
categories: [Minutes]
projects: [solo]
---
# Orphan Sync
This doc belongs to `Minutes` but NOT to the sibling category
`Team`. The level-2 axis (sibling `team`) is missing, so per the A1
fallback rule the path collapses to Minutes > project > Repository.
