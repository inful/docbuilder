version: "2.0"

repositories:
  - name: test-repo
    url: PLACEHOLDER  # Will be replaced with temp repo path in test
    branch: main
    paths:
      - docs

hugo:
  title: Frontmatter Injection Test
  description: Test automatic front matter injection (editURL, repository metadata)
  base_url: http://localhost:1313/
  theme: relearn
  params:
    editURL:
      enable: true
      base: https://github.com/example/test-repo/blob/main

output:
  directory: PLACEHOLDER  # Will be replaced with temp output dir in test
  clean: true
