version: "2.0"

repositories:
  - name: hextra-math
    url: PLACEHOLDER  # Will be replaced with temp repo path in test
    branch: main
    paths:
      - docs

hugo:
  title: Math Documentation Test
  description: Test KaTeX math rendering in Hextra theme
  base_url: http://localhost:1313/
  theme: hextra
  params:
    navbar:
      displayTitle: true
    footer:
      displayPoweredBy: false
    # Math support is automatically enabled by Hextra theme

output:
  directory: PLACEHOLDER  # Will be replaced with temp output dir in test
  clean: true
