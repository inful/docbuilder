package daemon

// Removed legacy fallback: Prometheus always enabled now. File retained empty to avoid go build issues
// during transitional cleanup; safe to delete once downstream modules stop referencing old paths.
