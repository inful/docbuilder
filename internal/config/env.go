package config

// Note: Environment file loading is now handled by CLI commands in
// cmd/docbuilder/commands/common.go::LoadEnvFile().
// This keeps the config library pure and separates side effects into CLI layer.
